<nav class="navbar navbar-expand-lg navbar-light bg-light <%= 'active font-weight-bold' if current_page?(root_path) %>">
  <%= link_to "HOME", root_path, class:"navbar-brand"%>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
      <li class="nav-item <%= 'active font-weight-bold' if current_page?(tdas_path) %>">
        <%= link_to "Tests", tdas_path, class:"nav-link" %>
      </li>
      <li class="nav-item dropdown <%= 'active font-weight-bold' if current_page?(doctors_path) || current_page?(new_doctor_path) %>">
        <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
          Informacion Doctores
        </a>
        <div class="dropdown-menu">
          <%= link_to "Listado de Doctores", doctors_path, class:"dropdown-item" %>
          <%= link_to "Ingresar nuevo doctor", new_doctor_path, class:"dropdown-item" %>
        </div>
      </li>
      <li class="nav-item dropdown <%= 'active font-weight-bold' if current_page?(patients_path) || current_page?(new_patient_path) %>">
        <a class="nav-link dropdown-toggle" href="#" role="button" data-toggle="dropdown" aria-expanded="false">
          Informacion Pacientes
        </a>
        <div class="dropdown-menu">
          <%= link_to "Listado de Pacientes", patients_path, class:"dropdown-item" %>
          <%= link_to "Crear nuevo paciente", new_patient_path, class:"dropdown-item" %>
          <div class="dropdown-divider"></div>
          <a class="dropdown-item" href="#">Something else here</a>
        </div>
      </li>
      <li class="nav-item">
      
      </li>
      <li class="nav-item">
        <% if user_signed_in? %>
        <p class="nav-link">
          <% unless current_page?(root_path) %>
            Logged in as <strong><%= current_user.name %></strong> -mn: <strong><%= current_user.mn %></strong>
            <%= link_to 'Edit profile', edit_user_registration_path %> |
            <%= link_to "Logout", destroy_user_session_path, method: :delete %>
          <% end%>
        <% else %>
          <%= link_to "Sign up", new_user_registration_path %> |
          <%= link_to "Login", new_user_session_path %>
        <% end %>
        </p>
      </li>
    </ul>
    
    <form class="form-inline my-2 my-lg-0">
      <input class="form-control mr-sm-2" type="search" placeholder="Search" aria-label="Search">
      <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
    </form>
  </div>
</nav>